{
    "name": "crity-uberfunc",
    "version": "1.0.0",
    "description": "Function that handles client request. Idea: Having a megafunc with switch-case is easier to deploy than lots of micro-functions.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Team Crity",
    "license": "MIT",

    "dependencies": {
        "node-appwrite": "^2.5.0"
    }
}
